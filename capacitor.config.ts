import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ionic',
  appName: 'ionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
